# karate BARシステム — 調査・改修メモ

このファイルには、ループエンジニアリングのラウンドごとに「何を試したか／結果は
どうだったか／何を学んだか」を追記していく。同じ調査を毎回ゼロからやり直さない
ための資産。

**別セッションでこのプロジェクトを引き継いだ場合は、まずファイル最後尾の
「引き継ぎサマリー」を読むこと。** 各ラウンドの詳細はその前に時系列で並んでいる。

---

## ラウンド1(2026-07-16): カメラ到着後の初期検証

### 目的
カメラ(Logitech C922 Pro Stream Webcam)を購入し実験可能になったため、
`CHECKLIST.md`/`VERIFICATION_AFTER_CAMERA.md`のうち、人間の現場作業を
必要としない項目(自動化可能な範囲)を検証する。

### やったこと
1. 環境整備: Python 3.13(`python`コマンド。`python3`はWindowsAppsの
   壊れたスタブなので使わないこと)、FFmpeg、requirements.txt一式を確認・導入
2. Mockモード回帰テスト → 問題なし
3. 実カメラでのPhase1確認(起動・クリップ生成)→ 起動はできたが…
4. 実測でフレームレートを計測 → **設定は60fpsなのに実測は約29.4fpsしか
   出ていないことを発見**
5. 原因切り分け: OpenCVのバックエンド(CAP_DSHOW vs CAP_MSMF)と
   FOURCC設定順序の組み合わせを比較実験 → `CAP_MSMF`+FOURCCを
   解像度/fpsより先に設定、で実測55〜60fpsに改善することを確認
6. `src/camera_source.py`の`RealCameraSource.start()`を修正、再検証
7. 長時間安定性テスト用の監視ハーネス(`soak_monitor.py`、health API
   ポーリング+psutilでのメモリ計測+バッファ/クリップサイズ監視)を自作し、
   30〜40分規模のテストを複数回実施
8. その過程で **Web監査サーバー(Flask開発用サーバー)が長時間稼働+
   継続アクセス時に完全にハングする** ことを発見。py-spyでスレッドダンプを
   取得し、Werkzeugのアクセスログ出力がWindows上のコンソール出力
   (colorama)の内部ロックを介して詰まっていることを特定
9. `src/web_server.py`・`central_dashboard/app.py`でWerkzeugの
   アクセスログを抑制する修正を実施、再検証(10分・30分テストで
   ハング再発なしを確認)
10. 合わせて、Webサーバーの起動失敗(ポート競合等)が発生してもこれまで
    完全にサイレントだった問題に気づいたため、`src/main.py`に起動確認
    (`_wait_for_web_server`)と警告表示・監査ログ記録を追加

### 結果(発見した問題と対応)

| 問題 | 影響 | 対応 |
|---|---|---|
| 実カメラのfps設定が実際には反映されず約29fpsしか出ない | クリップの実時間範囲が想定の約2倍になり、同期精度が崩れる。この案件の核心に関わる不具合 | `camera_source.py`: `CAP_MSMF`+FOURCC先出しに変更。修正済み・検証済み |
| Web監査サーバーが長時間稼働+アクセス継続でハング | 録画は継続するため誰も気づけないまま、iPadから監査画面に一切アクセスできなくなる | `web_server.py`/`central_dashboard/app.py`: werkzeugのアクセスログを抑制。修正済み・検証済み |
| Webサーバーの起動失敗がサイレントに握りつぶされていた | 起動時にポート競合等が起きても誰も気づけない | `main.py`: 起動後に実際にHTTP応答があるか確認し、無ければ警告+監査ログ記録。修正済み・検証済み |

### 未解決・要継続観察

- 30分テストの終盤約90秒間だけ、フレームレートが24fps程度まで一時的に
  低下する事象を1回観測。エラーは出ておらず録画・Webサーバーとも継続動作。
  USB電源管理等を疑っているが未特定。**本番前に数時間規模の連続稼働で
  再現するか確認することを推奨**(現状は30分規模の検証のみ)。

### 学んだこと・次回以降への申し送り

- このWindows環境では`python3`コマンドは使えない(WindowsAppsストアの
  スタブにリダイレクトされ動作しない)。`python`コマンドを使うこと。
- `cv2.VideoCapture.get(CAP_PROP_FPS)`は「要求した値」を返すだけで、
  実際に配信されているフレームレートを保証しない。カメラ設定を変更した
  際は、必ず実測(`read()`を一定回数呼んで所要時間を計測)で確認すること。
  ログの数値を鵜呑みにしない。
- Flaskの開発用サーバー(Werkzeug)は「開発用」と警告が出る通り、
  本番の長時間・高頻度アクセスには注意が必要。今回はアクセスログ抑制で
  回避したが、8コート運用で中央ダッシュボードが頻繁にポーリングする
  運用を考えると、将来的に本格的なWSGIサーバー(gunicorn等、ただし
  Windows対応に注意)への切り替えも検討候補。
- 長時間バックグラウンドプロセスをテストする際、`subprocess.PIPE`で
  子プロセスの標準出力を溜め込んだまま読まないと、子プロセス側の
  print()が大量に出るケースでパイプが詰まり、予期しない副作用
  (今回のハング原因の一因)を引き起こしうる。次回同様のテストをする際は
  子プロセスの標準出力を都度読み捨てるスレッドを用意するか、
  ファイルにリダイレクトすること。

### 現在の状態

- `data/clips/`・`data/buffer/`はテスト後にクリーンアップ済み
- `data/audit_log.jsonl`にはテスト中のイベントが残っている
  (本番前にクリアするか判断すること)
- 画質確認(実際の技の動きをカメラで撮影しiPadでコマ送り確認)は
  孝さん本人が実施し、「無事に動作しています」と確認済み(2026-07-16)

### 残っている人間の現場作業(未着手)

- 業者のUSBボタン到着後の同期精度実測(`VERIFICATION_AFTER_CAMERA.md`のC)
  → まず`tools/detect_keyboard_key.py`でキー名確認から
- USBケーブル抜き差しによる障害シナリオの実地確認(同D、F)
  → 現状の実装では、カメラ再接続後の自動復帰はしない設計のまま
    (`recorder.py`の`_capture_loop`はVideoCaptureを再オープンしない)
- 8コート運用時の複数台同時稼働確認(同G、複数PC・カメラが必要)

---

## ラウンド2(2026-07-18): クリップ永久保存機能の追加(応用性への第一歩)

### 目的

「良かったプレイを消えないように保存し、将来的に他の生徒・他の部活動とも
共有できる基盤にしたい」という孝さんの要望を受け、現行のFIFO(直近2回分
のみ保持)とは別に、監査担当が選んだクリップだけを永久に残せる仕組みを
追加する。将来的には空手専用ではなく他の部活動システムからも簡単に
繋げられる独立サービスに発展させたい、という大きな方向性が背景にある。

### 事前確認(AskUserQuestionで孝さんに確認した3点)

大きな設計判断が絡むため、実装前に以下を確認してから着手した:

1. **共有の置き場所・範囲**:「校内LAN限定の集約サーバー」を選択
   (現行の「クラウド送信は一切なし」という設計方針を維持する方向)
2. **今回のセッションでの実装範囲**:「最小機能のみ(保存ボタン+永久保存
   フォルダ)」を選択。校内LAN集約サーバーや複数部活動との連携は
   次ラウンド以降の課題として切り分けた
3. **未成年(生徒)映像の生徒間共有について学校側の方針**:「既に整理済み
   (保護者同意・校内規定を含め)。生徒間シェアも含めて設計してよい」との回答

→ この回答を踏まえ、今回は①の方向性を見据えつつ②の範囲だけを実装した。
③により、次ラウンド以降で生徒間シェアの設計に進んでよいという前提を得た
(ただし実際の同意取得方法・記録の所在などは孝さん側で管理されている前提。
 このシステム側では「誰の映像か」の識別・同意管理はまだ一切実装していない
 ことに注意)。

### やったこと

1. `src/config.py`に`SAVED_DIR`(`data/saved/`)・`SAVED_INDEX_PATH`
   (`data/saved/index.jsonl`)を追加
2. 新規モジュール`src/saved_clips.py`を作成。既存のFIFO管理
   (`clip_extractor.py`)には一切手を加えず、「`data/clips/`の確定クリップを
   `data/saved/`へコピーする」という完全に独立した処理にすることで、
   既存のクリップ生成・削除ロジックへの影響をゼロに抑えた
3. `src/web_server.py`に3つのAPIを追加:
   `POST /api/clips/<filename>/save`(保存)、
   `GET /api/saved`(一覧)、`GET /saved/<filename>`(配信)
4. `static/index.html`に「★ このクリップを保存」ボタンと「保存済み一覧」
   トグル表示を追加(選択中のクリップが既に保存済みの場合はボタンを隠す
   ことで、無意味な二重保存を防止)
5. `tests/test_save_clip.py`を新規作成し、
   「保存→一覧に出る→動画配信される→FIFOで元クリップが消えても
   保存済みコピーは残る」までを自動検証するテストを追加
6. 既存の`tests/test_pipeline_mock.py`・`tests/test_full_stack.py`が
   引き続き通ることを確認(既存ロジックへの影響がないことの裏付け)
7. Mockモードでサーバーを起動し、ブラウザ(Claude Browser)で実際に
   保存ボタン押下→保存済み一覧への反映→保存済みクリップの再生
   までをUI操作で確認(`read_page`/`computer`のref経由クリックが
   このセッションでは安定して動かなかったため、JavaScript直接実行
   経由でクリックイベントを発火させて検証した。動画の`readyState`が
   `HAVE_ENOUGH_DATA`になることまで確認済み)

### 実装中に見つけたバグ・その対応

| 問題 | 影響 | 対応 |
|---|---|---|
| 保存ファイル名に`COURT_NAME`(既定値"コート1"などの日本語)をそのまま使っていた | ファイル名がURLパスの一部になるため、日本語を含むと`urllib`等ASCII前提の処理が`UnicodeEncodeError`で落ちる(ブラウザの`fetch`/`<video src>`は自動エンコードするため気づきにくいが、他クライアントや将来のLAN集約サーバー連携で問題化しうる) | `saved_clips.py`: ファイル名にはコート名のSHA1ハッシュ先頭8文字(ASCII専用)を使うよう変更。人が読めるコート名は`index.jsonl`側の`court`フィールドに別途保持することで、UI表示上は従来通り日本語のコート名が見える |

### 学んだこと・次回以降への申し送り

- Claude Browser側の`computer`ツールで`read_page`から得た`ref`を使って
  クリックしようとすると、このセッションでは`screenshot`がタイムアウトし、
  `left_click`も"ref is stale"で失敗し続けた(定期ポーリング処理を
  止めても再現)。原因切り分けより検証を優先し、
  `javascript_tool`で直接`element.click()`を発火する方式に切り替えて
  検証を完了させた。次回同様の状況になったら、早めにこの代替手段に
  切り替えると時間を節約できる
- ファイル名やURLパスに使う識別子は、たとえ現状ASCIIでも「将来
  設定次第で日本語が入りうる」ものは早めにハッシュ化/サニタイズしておく
  べき、という教訓(`COURT_NAME`は`config.py`でユーザーが自由に
  書き換えられる値であり、実際に既定値が日本語だった)

### 現在の状態

- `data/`配下は今回のテスト・検証で作られたファイルをすべて削除済み
  (ラウンド1時点から残っていた`bar_clip_20260716_222700_853_0.mp4`
  ―孝さんが実機画質確認で使ったと思われるファイル―はそのまま残してある)
- `data/saved/`は空の状態(index.jsonlも生成前)

### 次ラウンド以降の候補(今回は着手していない)

- 校内LANの集約サーバー(`central_dashboard/`と同じ「別プロセス・別PC」
  パターンを踏襲する想定)へ、各コートの`data/saved/`を送る仕組み
- 空手以外の部活動システムから同じ「保存」の仕組みに接続する方法の設計
  (どこまでを「karate_bar本体」に残し、どこからを独立サービスとして
  切り出すかの線引きが必要)
- 生徒間シェアのUI・閲覧権限・生徒ごとの識別/タグ付け(今回は「保存済み
  一覧を時系列で見るだけ」の最小機能にとどめている)
- 保存クリップの容量増大への対応(現状、保存は無制限に増え続ける設計。
  ディスク容量の監視や大会後のアーカイブ運用について要検討)

---

## ラウンド3(2026-07-18): GitHubへの移行(ノートパソコンでの実行に向けて)

### 目的

「このアプリをノートパソコンで実行することを考えている。簡単に移行できる
ようにしてほしい」という要望を受け、これまでGit管理されていなかった
ローカルフォルダをGitHub経由で同期できるようにした。

### 事前確認(AskUserQuestionで確認した2点)

1. **移行方法**:「GitHubに上げて、今後はgitで同期」を選択
   (USBコピーやクラウド同期フォルダではなく、git clone/pull/pushを採用)
2. **ノートパソコンの状態**:「Python・FFmpegはすでに入っている」
   → セットアップ手順の1・2(Python/FFmpeg導入)は省略し、
     `git clone` + `pip install` のみで済む前提でREADMEを書いた

### やったこと

1. `.gitignore`を作成。`data/`(録画バッファ・確定クリップ・永久保存
   クリップ・監査ログ)を除外対象にした(映像データ・個人情報を
   GitHubに一切上げないための意図的な設計判断)
2. `git init` → 全ファイルをステージし、`git status`で`data/`が
   除外されていることを確認してからコミット(初回コミット)
3. GitHubのリポジトリ作成自体は孝さん本人に依頼した
   (`gh` CLIが環境に無いことに加え、孝さんのGitHubアカウントへの
   ログインを私が代行するのは避けるべき領域と判断したため。
   空のprivateリポジトリを作ってもらい、URLを共有してもらう形にした)
4. `https://github.com/kamuratakashi11/karate_var` をリモートに登録し、
   ブランチ名を`main`にリネームしてpush → 成功(認証は既存の
   Git Credential Manager等で自動的に通った模様、追加のログイン操作は
   発生しなかった)
5. READMEに「他のPC(ノートパソコンなど)への移行」節を追記:
   - `git clone` → `pip install -r requirements.txt` の手順
   - `data/`はgit管理外のため、過去の保存クリップ自体を持っていきたい
     場合はUSB等で別途手動コピーが必要なことを明記
   - `config.py`の機種依存設定(`BUTTON_KEY_NAME`・カメラのデバイス
     番号)はPCごとに実機再確認が必要なことを明記。`COURT_NAME`は
     既に環境変数で上書き可能な設計になっているため、複数PCで
     `config.py`本体を共有しつつコートごとに変えたい場合はこちらを
     使うと同期の衝突が起きにくい、という指針も書き添えた
6. 変更(`.gitignore`・README追記)をコミット・push済み

### 学んだこと・次回以降への申し送り

- この環境には`gh`(GitHub CLI)が入っていない。GitHub上でのリポジトリ
  作成のような「孝さんのアカウントでの操作」が必要な場面では、
  孝さん自身にブラウザで操作してもらい、結果(URLなど)だけを
  もらう形にするのが安全(ログイン代行はしない)
- `config.py`は複数PCでの共有を始めた時点で、「PCごとに変えたい設定」
  (カメラ番号・ボタンキー名など)と「揃えたい設定」が混在する
  ファイルになる。今回は`COURT_NAME`だけ環境変数化されている状態を
  活かして案内したが、複数コート運用が本格化したら、他の機種依存設定
  (`BUTTON_KEY_NAME`等)も環境変数化するかどうかは改めて検討の余地あり

### 現在の状態

- `C:\Users\k_tak\Downloads\karate_bar\karate_bar` はGitリポジトリ化済み、
  `main`ブランチが`https://github.com/kamuratakashi11/karate_var`と同期済み
- ノートパソコン側ではまだ何も作業していない(次にやること:
  上記READMEの手順で`git clone`してもらい、実際に起動確認)

---

## ラウンド4(2026-07-21): 実カメラでの一気通貫検証+試合単位の管理機能

### 目的

1. 実カメラでラウンド2・3の変更(保存機能・Git移行)が問題なく動くか確認する
2. 「良いプレーを他の生徒とシェアする」という当初のビジョンを詰めていく中で
   出てきた、「空手以外の競技(『やめ』で止まらないスポーツ)」「1試合の中で
   複数回やめが起きるケース」「複数試合をまたいだ振り返り」という新しい
   要件に対応する

### 実カメラでの検証結果

- 起動・撮影・「やめ」でのクリップ確定・保存(★ボタン)まで一通り実施し、
  すべて正常動作を確認
- 実測: 1280x720/60fps(ラウンド1のfps修正は今も有効)、クリップ長9.33秒
  (目標10秒に対し、録画開始直後だったための正常な短縮)
- **発見・修正したバグ**: 保存済み一覧(`#saved-bar`)に`max-height`+
  `overflow-y:auto`を付けたところ、Flexboxの「自動最小サイズ」計算の
  副作用で、画面が縦に狭いとその要素自体が極端に押しつぶされる不具合が
  発生。`max-height`/`overflow-y`をやめ、代わりに`flex-shrink: 0`を
  付けることで解決。ただしこれにより今度は**同じ問題がslot-bar側に
  移動**して再現(先に固定した要素の代わりに、固定していない別の
  兄弟要素が押しつぶされる)。`#slot-bar`・`#saved-toggle-row`にも
  同様に`flex-shrink: 0`を付けて解決。
  **学び**: Flexboxコンテナ内で`overflow`を`visible`以外にすると、
  その要素の「コンテンツに基づく自動最小サイズ」が0扱いになり、
  画面が狭いときに他の兄弟より優先的に圧縮される。複数の「絶対に
  潰れてほしくない」要素がある場合、場当たり的に1つずつ直すのではなく、
  最初から関係する要素すべてに`flex-shrink: 0`を検討すべきだった
- Flaskの`render_template`はデバッグモードOFF時にテンプレートをキャッシュし、
  `static/index.html`を書き換えてもサーバー再起動まで反映されないことが
  判明(検証中、修正が効かず原因調査に時間を使った)。次回同様の
  UI検証をする際は、最初から「HTML/JSを直したら毎回プロセス再起動が
  必要」という前提で動くこと

### 要件を詰める過程で分かったこと(孝さんとの対話)

- 「保存済み一覧が2件」は、実は**2つの別物**だった:
  (1) ライブの「やめ」バッファ(`CLIP_SLOTS`、旧デフォルト2)と
  (2) 永久保存(`data/saved/`、元々無制限)。孝さんが増やしたいと
  考えていたのは主に(1)で、(2)はラウンド2の時点で既に無制限だった
- 実測ベースの容量試算: 実カメラの10秒クリップは平均約5.9MB。
  10GBの空き容量で約1,700クリップ相当。多くの部活動が長期間使っても、
  容量そのものが近い将来ボトルネックになる可能性は低いと判断
- 空手であっても「1試合中に複数回やめが起き、試合後にまとめてレビュー
  したい」ケースがあり、CLIP_SLOTS=2では実質足りないという指摘があった
- 結論として、「件数を増やす」だけでは根本解決にならない
  (レビューが追いつく前にFIFOで消える構造自体は残るため)ことを共有し、
  「試合」という単位でクリップをグループ化し、試合が変わるまでは
  積極的に削除しない、という設計に方向転換した
- 将来的に、この「試合番号」は空手タイマーシステム側が既に保持している
  試合番号を読み取る方式に差し替える構想がある(選手名ではなく試合番号を
  キーにする、と孝さんから明言があった)。今回はまだそのシステムとの
  実際の連携仕様が分からないため、「試合番号の設定経路を1箇所にまとめる」
  というだけの設計にとどめ、実際の読み取り連携は着手していない

### やったこと(実装)

1. `src/config.py`: `CLIP_SLOTS`の既定値を2→20に変更、環境変数で
   上書き可能に(`COURT_NAME`と同じパターン)
2. `src/clip_extractor.py`:
   - `_slots`の各要素に`"match"`(試合番号)を追加。`next_match()`/
     `set_match()`/`get_current_match()`で試合番号を進める・設定する
     経路を1箇所にまとめた(将来のタイマー連携時はここだけ差し替える想定)
   - `list_current_clips()`の戻り値をパス文字列のリストから
     `{"path":..., "match":...}`のリストに変更(呼び出し側
     `web_server.py`・既存テストも合わせて修正)
   - `get_match_for_clip(filename)`: 保存時にそのクリップの試合番号を
     引くためのメソッドを追加
   - `clear_all()`: ライブクリップを一括削除するメソッドを追加
     (`data/saved/`側には一切触れない)
3. `src/saved_clips.py`: `save_clip()`に`match`引数を追加。保存時に
   元クリップの試合番号を引き継いでメタ情報に記録
4. `src/web_server.py`: `POST /api/match/next`・`GET /api/match/current`・
   `POST /api/clips/clear`を追加。`/api/clips`のレスポンスに`match`を追加
5. `static/index.html`:
   - 「試合N」表示+「次の試合へ →」ボタン+「ライブ一括削除」ボタン
     (削除は`confirm()`で確認。次の試合への遷移は非破壊的操作のため
     確認なしにした)
   - ライブ一覧・保存済み一覧の両方を試合番号でグループ表示
     (`.match-header`を`flex-basis:100%`にして折り返しの区切りに利用)
6. 既存テスト2本(`test_pipeline_mock.py`・`test_save_clip.py`)を、
   `CLIP_SLOTS`の既定値変更に合わせて修正(テスト内で明示的に
   `os.environ["CLIP_SLOTS"]="2"`を設定し、FIFOの検証自体は維持)
7. 新規テスト`test_match_and_clear.py`を追加。試合タグ付け・試合を
   跨いでも削除されないこと・保存時の試合番号引き継ぎ・一括削除で
   ライブのみ消えて保存済みは残ることを自動検証
8. Mockモードでブラウザから実際に操作し、グループ表示・保存・試合切替・
   一括削除まで一通り目視確認済み

### 現在の状態

- 上記の変更はすべてローカルには反映済みだが、**まだgitコミット・
  pushはしていない**(孝さんの確認待ち)
- `data/`配下はテストで作られたファイルをすべて削除済み

### 追記: 「次の試合へ」のNキー割り当て(同ラウンド内、コミット前に追加)

孝さんから「次の試合へボタンはキーボードのNキーに割り当ててほしい」と
依頼があり、コミット前に追加で対応した。

- `src/config.py`: `NEXT_MATCH_KEY_NAME`(既定"n"、環境変数で上書き可)を追加
- `src/key_listener.py`: `NextMatchKeyListener`を新規追加。F2トグル判定
  (`TimerSyncedKeyListener`)とは無関係な、独立したpynputグローバルフックとして
  実装(`--input-mode enter`/`button`のどちらでも常時有効)。既存の
  `handle_key_event`パターンを踏襲し、pynput無しでロジック単体をテスト
  できるようにした
- `src/main.py`: 常時`NextMatchKeyListener`を起動するよう変更
- **実装中に見つけて直したバグ**: `--input-mode enter`時、Nキーで試合を
  進めた直後にEnterを押すと、ターミナルの`input()`が拾った"n"という
  文字列が(内容を見ずに)「やめ」としても発火してしまい、試合送りと
  同時に不要なクリップが1本できてしまう二重発火が起きることに気づいた。
  `input()`の戻り値が本当に空文字列の場合だけ「やめ」を発火するよう修正
- `tests/test_next_match_key.py`を新規追加(カメラ・ffmpeg不要の高速テスト。
  該当キーでの発火・別キーでの無視・デバウンスを確認)。実行して全て
  パスすることを確認済み。`main.py --mock`の起動スモークテストも実施し、
  「[main] 「次の試合へ」キー: N」のログ表示とエラー無しでの起動・終了を確認

### 次ラウンド以降の候補

- ディスク空き容量が少なくなった際の警告機能(既存の`on_warning`の
  仕組みを再利用する想定。今回は時間の都合で見送った)
- 空手タイマーシステムから試合番号を実際に読み取る連携
  (連携方法・仕様がまだ不明なため、まずタイマーシステム側の調査が必要)
- 校内LANの集約サーバー、他部活動システムとの接続方法、生徒間シェアUI
  (ラウンド2・3から持ち越し、引き続き未着手)

---

## ラウンド5(2026-07-21): オートフォーカス制御+ライブプレビュー画面

### 目的

実カメラでの検証中(ラウンド4)、オートフォーカスが試合中に迷って画面が
ぼやける挙動が確認された。これに対応するため、
1. オートフォーカスをオフにできるようにする
2. カメラのセッティング(画角・ピント)を確認するためのライブプレビュー画面を追加する
の2点を実装する。この2つは「プレビューを見ながら適切な手動フォーカス値を
決め、それを固定値として設定する」という一連の流れになる想定。

### やったこと

1. `src/config.py`: `AUTOFOCUS_ENABLED`(既定True、環境変数で上書き可)・
   `MANUAL_FOCUS_VALUE`(既定None)・`PREVIEW_JPEG_QUALITY`を追加
2. `src/camera_source.py`: `RealCameraSource.start()`で`CAP_PROP_AUTOFOCUS`・
   `CAP_PROP_FOCUS`を設定し、`.get()`で実際に反映された値をログ出力する
   ようにした(ラウンド1のfps問題と同様、`CAP_MSMF`バックエンドで実際に
   効くかはドキュメントだけでは確証が持てないため、要実機検証)
3. `src/recorder.py`: 録画スレッドが直近1フレームをロック付きでメモリに
   保持するようにし、`get_latest_frame_jpeg()`でJPEGエンコードして
   取り出せるようにした(録画用ffmpegパイプラインとは別経路。カメラを
   二重に開かずに済むよう、既存の録画用フレーム取得をそのまま再利用する設計)
4. `src/web_server.py`: `GET /preview`(プレビュー画面)・
   `GET /api/preview.jpg`(直近フレームのJPEG、`Cache-Control: no-store`)を追加
5. `static/preview.html`を新規作成。0.4秒間隔でJPEGをポーリングして
   `<img>`を差し替える方式(完全な動画ではなく、コマ送りに近い「ほぼライブ」
   表示。カメラのセッティング確認用途であり、試合中ずっと開いておく前提ではない)
6. `static/index.html`に「📷 カメラプレビュー」リンクを追加(新規タブで開く)
7. `src/main.py`起動ログにプレビューURLの案内を追加
8. `tests/test_preview.py`を新規作成。JPEGが正しいマジックバイト/終端
   マーカーを持つか、時間経過でフレーム内容が更新され続けているか、
   `/preview`画面が配信されるかを自動検証。全てパス
9. 既存の回帰テスト(`test_pipeline_mock.py`・`test_full_stack.py`)も
   再実行し、`recorder.py`の変更が既存動作に影響していないことを確認
10. Mockモードでブラウザから`/preview`を開き、1280x720の映像が表示され
    続けること、監査画面からのリンクが機能することを目視確認

### 実装中に起きたこと: ラウンド1由来ファイルの誤削除

テスト後片付けのコマンドで`bar_clip_202607*.mp4`という広すぎる
ワイルドカードを指定してしまい、当日分のテストファイルと一緒に、
ラウンド1から残していた`bar_clip_20260716_222700_853_0.mp4`
(実カメラでの画質確認用ファイル。過去3ラウンドにわたって「消さないように」
と注意していたもの)まで誤って削除してしまった。`data/`は`.gitignore`
対象のためGit履歴からの復元もできず、実質的に復旧不可能だった。

孝さんに確認したところ「テストデータなので問題ない」との回答で実害は
なかったが、**危うく孝さん自身が確認した記録を失うところだった**。
教訓として、削除コマンドは実行前に対象ファイルを`ls`等で個別確認してから
使うこと(広いワイルドカードで「今日作ったはずのファイル」を消すつもりが、
たまたま日付文字列が部分一致する古いファイルまで巻き込むリスクがある。
今回で言えば"20260716"が"202607"に部分一致した)。

### 現在の状態

- 上記の変更はローカルには反映済みだが、**まだgitコミット・pushはしていない**
- `data/`配下はテストで作られたファイルを削除済み(前述の誤削除により、
  ラウンド1由来のファイルは現在残っていない)
- **実機(実カメラ)でのオートフォーカス制御・プレビュー画質の確認は
  まだ行っていない**。次にやること: 実カメラで起動し、
  (1) `/preview`で画角・ピントを目視確認、(2) 手動フォーカス値を
  環境変数`MANUAL_FOCUS_VALUE`で調整しながら追い込む、
  (3) `AUTOFOCUS_ENABLED=0`にしてオートフォーカスのボケが実際に
  収まるか確認、という順で進める想定

---

## ラウンド6(2026-07-22): 「再生速度が速い」「映像の終端が壊れている」の調査・対応

### 目的

孝さんが実カメラでラウンド5の機能を試している最中に発見した2件の不具合報告
(「再生が実際より早送りに見える」「映像の最後がおかしい」)を調査する。

### fps低下(再生速度が速い件)の調査

一連の実機測定(すべて実カメラ、`/api/health`のframe_count/uptimeで実測):

| 検証内容 | 結果 |
|---|---|
| 実際に稼働していたセッション(約5分) | 平均29.1fps(設定は60fps) |
| ラウンド5のオートフォーカス設定の有無 | 影響なし(58.35 vs 58.44fps) |
| エンコード(libx264 ultrafast)単体の速度 | 700fps超、全く問題なし |
| GPU(NVENC、RTX 2060 SUPER搭載)に切り替え | 変化なし(そもそもボトルネックではなかった) |
| 静止状態で2分間の連続稼働 | 50〜51fpsで安定、劣化なし |
| **実際に体を動かして2分間** | **57.7fps(平均)、後半は60.0〜60.2fpsで安定** |

**結論: 原因を特定できなかった。** 候補にしていた仮説(私自身の変更、
エンコード速度、GPU、経過時間、動きの激しさ)はすべて再現テストで否定された。
ラウンド1で記録していた「30分テストの終盤に1回だけ24fpsまで低下、原因未特定」
と同種の、狙って再現するのが難しい間欠的な現象と考えられる。

孝さんの判断で、**今回は原因究明を保留**し、fps低下時のデータは孝さん側で
保存の上、後日提供いただく方針になった。fps低下を検知して警告する機能の
追加も提案したが、今回は見送り(次回以降の課題)。

途中の副次的な発見: `.get(CAP_PROP_AUTOFOCUS)`が、設定値に関わらず常に0.0を
返す(=このカメラ・CAP_MSMFの組み合わせでは、この値が正しく読み戻せない
可能性がある)。ラウンド5で保留していた「オートフォーカス制御が実際に
効くか」の実機確認は、まだ決着していない。

### 映像破損(終端がおかしい件)の調査と対応

実際に生成された3本のクリップをすべてffmpegでデコード検証したところ、
**全てにH.264デコードエラー(マクロブロック破損)を確認**。発生位置は
いずれもクリップ終端付近だった。

**原因**: ラウンド1の設計で、タイマー同期精度を上げるため「今まさに
書き込み中の最新セグメント」もクリップに含めている。この読み取りと、
録画側ffmpegによる同じファイルへの書き込みが同時に走るため、書きかけの
不完全なデータを読んでしまうことがある(レース条件)。「TS形式は書き込み中
でも安全に読める」という設計上の前提は、ファイルシステム的な安全性は
保証していても、H.264ビットストリームとしての完全性までは保証していなかった。

**対応(実装済み)**: `src/clip_extractor.py`の`extract_on_yame()`を、
以下のフォールバック方式に変更した。

1. 従来通り(書き込み中セグメントも含める、同期精度優先)でクリップを作る
2. 生成直後に実際にデコードして検証する(`ffmpeg -v error -i ... -f null -`
   でエラー出力の有無を見るだけの軽量なチェック)
3. 壊れていたら、その最新セグメントを除外した安全な方式で作り直す
   (この場合のみ同期精度が最大`SEGMENT_SECONDS`秒ほど犠牲になる)
4. 作り直しが発生したことは監査ログに`clip_corruption_fallback`
   (`still_broken`フィールド付き)として記録する
5. 万一作り直し後もまだ壊れていた場合は、無限リトライはせず警告を出して
   そのまま返す(取れる映像自体は返す。原因調査が別途必要という扱い)

**検証**:
- `tests/test_clip_corruption_fallback.py`を新規作成。`_has_decode_errors`
  をモック化し、「1回目破損→作り直しで正常」「1回目・2回目とも破損→
  諦めて警告のみ、無限ループしない」の両パターンを自動検証。パス
- 既存の回帰テスト全て(`test_pipeline_mock.py`・`test_save_clip.py`・
  `test_match_and_clear.py`・`test_full_stack.py`)を再実行し、影響がないことを確認
- **実カメラで4回連続「やめ」を実施し、最終検証**: 4回とも1回目は
  破損していたが、4回ともフォールバックで正常なクリップに作り直され、
  最終的に配信される4本すべてがデコードエラーなしと確認できた

**申し送り**: 現状、実カメラでは**ほぼ毎回1回目が破損し、フォールバックが
発動している**(4/4回)。つまり今のところ、ラウンド1が意図した「書き込み中
セグメントを含めた高精度」の恩恵はほとんど実現できておらず、実質的に
常に「安全な方式(最大2秒精度)」で動いている状態に近い。読み取り競合
そのものの発生頻度を下げる改善(例: 録画側の書き込みタイミングとの
すり合わせ)は、今回は着手していない。

### 現在の状態(このラウンド最初の対応時点)

- `clip_extractor.py`・新規テストの変更はローカルには反映済みだが、
  **まだgitコミット・pushはしていない**
- `data/`配下はテストで作られたファイルを削除済み
- fps低下の根本原因は依然として未解決。孝さんからのデータ提供待ち

---

### 追記: 破損対策をフォールバック方式から「書き終わり待ち」方式に変更

上記の対応(壊れたら作り直すフォールバック方式)を報告したところ、
孝さんから「やめの後0.5秒待ってから切り出し、その0.5秒分を後で
削ればいいのでは」という提案があった。

固定0.5秒では、セグメント切り替え直後に「やめ」が起きた場合に
足りない(最大で2秒近く待つ必要がありうる)ことを説明した上で、
「固定時間ではなく、書き込み中だったセグメントが実際に書き終わる
(次のセグメントが現れる)まで待つ」という、孝さんの発想を活かした
より正確な方式に設計し直すことで合意した。

**実装内容**:
1. `src/config.py`: `SEGMENT_CLOSE_WAIT_TIMEOUT_SEC`(既定
   `SEGMENT_SECONDS + 1.0`)を追加
2. `src/clip_extractor.py`:
   - `_wait_for_segment_to_close()`を新規追加。「やめ」の瞬間に
     書き込み中だったセグメントを特定し、それより新しいセグメントが
     現れる(=書き終わった)まで最大`SEGMENT_CLOSE_WAIT_TIMEOUT_SEC`秒待つ
   - `_build_clip()`を「anchor_segment(＝待って確認したセグメント)より
     新しいものは使わない」設計に変更(「やめ」より後の内容が紛れ込んで
     クリップの終端がずれることを防ぐため)
   - デコード検証によるフォールバック(壊れていたらanchor_segment自体も
     除外して作り直す)は、万一の保険としてそのまま残した
3. **重要な副次対応**: `extract_on_yame()`が最大数秒ブロックするようになる
   ため、物理ボタン(`--input-mode button`)使用時にpynputのキーボード
   フックのコールバック内で直接この待ちをすると、Windowsがフックを
   応答なしとみなして無効化する危険があると判断。`src/key_listener.py`の
   `TimerSyncedKeyListener._fire()`を、`on_yame`を別スレッドで実行して
   即座に呼び出し元へ返すよう修正した。これは孝さんの提案を実装する過程で
   新たに気づいた、対応が必須の問題だった
4. `tests/test_key_listener_nonblocking.py`を新規追加。
   `handle_key_event`が`on_yame`の完了を待たずに即座に返ることを確認

**検証結果**:
- 新規テスト・既存の回帰テスト全て(6本)がパス
- **実カメラで5回連続「やめ」を実施し、5回とも1回目から正常
  (フォールバック発動なし)を確認**。ラウンド6最初の対応(4/4回
  フォールバックが必要だった)と比べて、根本的に改善している。
  所要時間は1回あたり1〜1.25秒程度で実用上問題ない

### 現在の状態

- `clip_extractor.py`・`config.py`・`key_listener.py`・新規テスト2本の
  変更はローカルには反映済みだが、**まだgitコミット・pushはしていない**
  (孝さんが一度ご自身でテストしたいとのことで、その結果待ち)
- `data/`配下はテストで作られたファイルを削除済み
- fps低下の根本原因は依然として未解決。孝さんからのデータ提供待ち(継続)

---

## 引き継ぎサマリー(2026-07-22時点、別セッションへの引き継ぎ用)

### プロジェクトの場所

- 作業ディレクトリ: `C:\Users\k_tak\Downloads\karate_bar\karate_bar`
- Gitリポジトリ化済み。リモート: `https://github.com/kamuratakashi11/karate_var`
  (branch `main`)。ローカルの `main` は origin と同じコミット
  (`6326e86`、ラウンド3時点)から進んでおらず、**ラウンド4・5・6の変更が
  すべて未コミットのまま**ローカルに溜まっている状態。

### 今すぐやるべきこと

1. **ラウンド6(映像破損対策)の実機検証**。孝さんご自身で
   `cd src && python main.py --camera 0` を起動し、Enterキーで何度か
   「やめ」を試す。生成されたクリップが壊れていないか確認する方法:
   ```bash
   ffmpeg -v error -i data/clips/<ファイル名> -f null -
   ```
   何もエラーが出なければ正常。孝さんに引き継いだ直後の`data/clips/`に
   `bar_clip_20260722_062419_572_0.mp4`という1本が残っていた
   (孝さんご自身が直前に試された分と思われる)。私の方で
   デコード検証したところ正常(1280x720/60fps/10.0秒、エラーなし)だった。
   削除はせずそのまま残してあるので、これも参考にできる。
2. 検証してもらった内容(壊れていないか、体感的な使用感)を確認できたら、
   **ラウンド4〜6のすべての変更をまとめてコミット・push**する
   (孝さんに確認の上で実行すること。詳細は下記「今回セッションで変更した
   ファイル一覧」を参照)。
3. 物理ボタン(業者のUSB早押しボタン)がまだ未着手の場合、それが届いたら
   `--input-mode button`での動作確認(ラウンド1のCHECKLIST.md/
   VERIFICATION_AFTER_CAMERA.mdの残項目)も忘れずに。ラウンド6で
   `key_listener.py`のスレッド化を行った影響で、ボタン方式は今回初めて
   実機で試すことになる点に注意(理論上は問題ないはずだが未検証)。

### 保留中の別件(このセッションでは着手していない)

- **fps低下(再生が速く見える件)**: ラウンド6で調査したが原因を特定できず、
  孝さんの判断で保留中。**孝さんがfps低下時のデータを保存して後日提供する
  予定**なので、それが来たら調査を再開すること。詳細はラウンド6の
  「fps低下(再生速度が速い件)の調査」を参照。
- **オートフォーカス制御・ライブプレビューの実機での画質調整**
  (ラウンド5で機能は実装済みだが、実際にボケが収まるか・良い
  `MANUAL_FOCUS_VALUE`はいくつかを、`/preview`を見ながら現地で
  詰める作業がまだ)。副次的な発見として、`.get(CAP_PROP_AUTOFOCUS)`が
  設定値に関わらず常に0.0を返すことが分かっており、この値が
  正しく機能しているかどうか自体、実機の見た目で確認する必要がある。
- 校内LAN集約サーバー・他部活動連携・生徒間シェアUI(ラウンド2から持ち越し、
  引き続き未着手)。

### 今回セッション(ラウンド4〜6)で変更したファイル一覧

```
M README.md
M karate_bar_notes.md
M src/camera_source.py       (オートフォーカス制御)
M src/clip_extractor.py       (試合タグ付け・一括削除・破損対策)
M src/config.py                (各種設定追加)
M src/key_listener.py           (Nキー・スレッド化)
M src/main.py                    (Nキー配線・プレビューURL案内)
M src/recorder.py                 (ライブプレビュー用フレーム保持)
M src/saved_clips.py               (試合番号の引き継ぎ)
M src/web_server.py                 (試合・一括削除・プレビューAPI)
M static/index.html                  (試合UI・プレビューリンク・レイアウト修正)
M tests/test_pipeline_mock.py         (CLIP_SLOTS既定値変更への追随)
M tests/test_save_clip.py              (同上)
?? static/preview.html
?? tests/test_clip_corruption_fallback.py
?? tests/test_key_listener_nonblocking.py
?? tests/test_match_and_clear.py
?? tests/test_next_match_key.py
?? tests/test_preview.py
```

### 環境に関する申し送り(再掲・重要)

- Windows既定の`python3`コマンドは動かない。**`python`コマンドを使うこと**
- `cv2.VideoCapture.get()`(fps・オートフォーカス等)は「要求値」を返すだけで
  実際に反映されたかは保証しない。必ず実測・目視で確認すること
- テストデータ(`data/buffer/`・`data/clips/`・`data/saved/`)の削除コマンドは、
  **対象ファイルを`ls`等で個別確認してから**実行すること
  (ラウンド5で広いワイルドカードにより孝さんの確認用ファイルを誤削除した
  実例あり。実害はなかったが再発させないこと)
